# CONTEXT — 도메인 용어와 전제

코드와 요구사항 문서만으로는 알 수 없는, 합의된 도메인 사실의 글로서리.

## 용어

- **표준 샘플**: `bench/gen`으로 생성하는 50MiB CSV × 119개 tar.gz (총 행
  112,885,096). 모든 성능 판정의 기준 입력.
- **producer**: tar.gz 읽기 → gzip 해제 → tar 파싱 → 행 블록 분할까지의
  직렬 스트리밍 경로.
- **consumer**: DuckDB 스레드들이 수행하는 CSV 파싱 → chunk 적재 →
  Parquet 인코딩(zstd) 경로. 코어 수만큼 병렬.
- **행 블록**: producer가 행 경계(`\n`)에서 잘라 consumer에 넘기는 바이트
  덩어리. 블록 내 모든 행은 완전한 행이다.

## 환경 전제

- **입력 포맷 고정**: `A.tar.gz`(단일 스트림 gzip)는 외부 생산자가 만들며
  협상 불가. 병렬 해제 가능한 포맷(zstd, 블록 gzip)으로의 변경은 불가능.
- **실행 환경 스펙트럼**: 운영 머신은 1코어부터 수십 코어까지 다양하다.
  특정 코어 수를 가정한 최적화는 하지 않는다.
- **압축률 우선**: 출력이 parquet+zstd인 핵심 이유는 압축률이다. 속도를
  위해 압축 레벨을 낮추는 트레이드는 반려됨 (2026-08-07).
- **컬럼 수 감소 여지**: 향후 CSV의 컬럼 수가 줄어들 수 있다. 스키마는
  헤더에서 동적으로 결정되므로 컬럼 감소를 막는 가정을 코드에 넣지 않는다.
- **메모리 예산**: peak RSS는 데이터 크기와 무관한 고정 수준을 유지하되
  절대치 1GB 이내.
